#include <stdio.h>
#include <math.h>



long fibo(int n) {
    if (n == 0) {
        return 0;
    }else if (n == 1) {
        return 1;
    }
    assert(n <= 90); //n <= 90 が必要
    if (n % 2 == 0) {
        
    }
}